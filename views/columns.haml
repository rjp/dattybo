%table
 %tr
  %th Date
  - @data_vars.each do |d|
   %th #{d}, #{@info[d][:type]}
 %tr
  %td
  - @data_vars.each do |d|
   %td
    %img(src="#{@info[d][:graph]}")
    <!-- #{@info[d][:graph_data]} -->
 - @days.each do |i|
  %tr
   %td= i
   - @data_vars.each do |d|
    %td #{@info[d][:data][i]}
